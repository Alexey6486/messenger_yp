.add-modal-form {
	display: flex;
	flex-direction: column;
	justify-content: space-between;

	& > button {
		margin: 0 auto;
	}
}

.chat-user-list {
	position: absolute;
	top: 70px;
	left: 0;
	width: 100%;
	box-shadow: var(--shadow-btn);

	& > ul {
		margin: 0;
		padding: 0;
	}
}
